[
  {
    "name": "/help explains the bot in plain language",
    "steps": [
      {
        "send": { "text": "/help" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Browse available work, post a task, or check your accepted tasks.\n\nUse the buttons in the main menu to get started."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "the Help menu button shows the same help text",
    "steps": [
      {
        "send": { "callback": "menu:help" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
